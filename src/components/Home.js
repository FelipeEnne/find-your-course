import React from 'react';
import PropTypes from 'prop-types';


const Home = props => {
  const { user } = props;

  return (
    <div className="home">
      app
    </div>
  );
};



export default Home;